#include <stdio.h>

/**
 * main - entry point
 * Program that prints the lowercase alphabet in reversei.
 * Return: (0)
 */

int main(void)
{
	char alph;
	
	for (alph = 'z'; alp >= 'a'; alph--)
	{
		putchar(alph);
	}
	putchar('\n');
	return (0);
}
